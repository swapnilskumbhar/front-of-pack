import { getCloudflareContext } from "@opennextjs/cloudflare"; import { NextRequest } from "next/server"; import { OFFICER_COOKIE, redactAggregateRows, verifyOfficerSession } from "@/public-services";
interface Statement { all<T>():Promise<{results?:T[]}> } interface Db { prepare(query:string):Statement } interface Env { DB?:Db; OFFICER_SESSION_SECRET?:string }
export async function GET(request:NextRequest):Promise<Response>{let env:Env;try{env=(await getCloudflareContext({async:true})).env as Env}catch{return Response.json({error:"Unavailable."},{status:503})}if(!env.DB||!env.OFFICER_SESSION_SECRET||!await verifyOfficerSession(request.cookies.get(OFFICER_COOKIE)?.value,env.OFFICER_SESSION_SECRET))return Response.json({error:"Not found."},{status:404});const result=await env.DB.prepare("SELECT status, language, COUNT(*) AS count FROM analyses GROUP BY status, language ORDER BY status, language").all<Record<string,unknown>>();return Response.json({rows:redactAggregateRows(result.results??[]),redacted:true,notice:"Aggregate analysis counts only. No images, identifiers, model output, or profile data."},{headers:{"Cache-Control":"no-store"}})}
